<% if current_user %>
  Signed in as: <%= t('current_user', name: current_user.name) %>
  <%= button_to t('button.sign_out'),
    sign_out_path,
    method: :delete %>
<% else %>
  <%= link_to t('button.sign_in'), sign_in_path %>
<% end %>

